![Last Updated](https://img.shields.io/badge/Last_Updated-2026--04--06-blue.svg?style=for-the-badge)
![API Status](https://img.shields.io/badge/API_Engine-Triple_Fallback-success.svg?style=for-the-badge)
![Sheet Logic](https://img.shields.io/badge/Sheet_Logic-XLOOKUP-orange.svg?style=for-the-badge)

這是一個為 HILTI 專門設計的政府招標資料自動化工具，具備最高的抓取成功率。

## 🌟 核心功能
*   **三核心 API 備援系統**：自動排序 `WebScraping.AI` > `ScrapingAnt` > `scrape.do`，具備全球住宅代理，自動繞過所有擋牆。
*   **全方位防呆機制**：內建模糊比對演算法，自動偵測並攔截重複錄入。
*   **資料庫自動匹配**：J 欄自動寫入 `XLOOKUP` 公式連動 `Customer` 表格。
*   **視覺化數據儀表板**：首頁採用現代化 Grid 佈局，即時顯示 7 組 API 的進度環與剩餘額度。

## 📅 版本更迭紀錄

### [2026-04-06] - 數據中心儀表板究極版 V35
- **UI 現代化**：數據中心全面升級為「現代化儀表板 (Dashboard UI)」，採用玻璃擬態 (Glassmorphism) 風格與網格佈局，視覺質感大幅提升。
- **視覺化進度環**：為各 API Key 實作動態 SVG 進度環。透過 `stroke-dashoffset` 技術精確呈現額度剩餘百分比，並支援 `stroke-linecap="round"` 圓潤視覺。
- **智能顏色警報 (Smart Alerts)**：實作自動偵測機制：
    - 🟢 **額度充裕 (≥50%)**：顯示主題紫色/青色/綠色。
    - 🟠 **警戒狀態 (<50%)**：自動切換為橙色 (#ffab40)。
    - 🔴 **危險狀態 (<20%)**：自動切換為鮮紅色 (#ff1744)。
- **邏輯優化**：針對 WebScraping.AI API 缺失 `total` 欄位的特性，實施「自動補償機制 (Fallback)」，確保所有進度環皆能正確計算比例。
- **渲染加強**：導入雙重渲染 (Dual-Rendering) 機制，確保數據載入後進度環顏色能穩定顯現，排除跨瀏覽器兼容性問題。

### [2026-04-05]
- **功能改進**：擴充至 7 組付費 API（共 3 組 `WebScraping.AI`、3 組 `ScrapingAnt`、1 組 `scrape.do`），建構高達七道防線的終極自動接力備援機制，最大化抓取承載力。
- **UI 調整**：數據中心陣列結構升級為 3 欄式的高密度緊湊排版，卡片總庫充至 8 張。並透過精簡內邊距與視覺元件，完美保證所有監控卡片全數收納於無捲軸的單一螢幕內。

### [2026-04-05] - 書籤協同究極版 V33
- **架構重構**：捨棄總覽頁即時分析模式，全面轉型為「人機協作模式」，配合專屬書籤避開網站動態干擾，成功率直逼 100%。
- **防護機制**：實作「雙重防護架構」，第一層在前端以零成本直接與 Google Sheets 對接比對網址，瞬間「秒殺」已存標案，完美杜絕浪費付費 API 額度的問題。
- **功能改進**：推出「自動複製神偷書籤」，精準屏除排序網址等干擾雜訊，並調用 `navigator.clipboard` 實現一鍵提串、完全免動手自動複製。
- **UI 調整**：導入專業控管室級的「1:2 左右滿版分割視覺」，左側 (1/3) 緊湊配置網頁輸入區與 2x2 API 監控陣列；右側 (2/3) 展開廣闊的歷史日誌清單，打造免滾輪、零死角的高效工作區。
- **UI 調整**：微調狀態回報區塊的字級大小，並解除數據板塊的橫寬限制以完美貼齊主輸入框邊界，同時移除強制的比例枷鎖，大幅釋放垂直空間。
- **邏輯優化**：延長批次冷卻時間至 10 秒增加匿蹤能力；日誌區直接顯示付費代理來源提供核對；並透過後端正則表達式徹底清除日期欄位內的 `(預估)` 雜質殘留。

### [2026-04-05] - 三核心 API 究極版 V21
- **[重大更新]** **智慧省錢抓取模式 (Cost-Saving Fetch)**：新增「Level 0 直接抓取」機制。系統會優先嘗試免費連結，僅在偵測到被標案網阻擋 (驗證碼) 時，才會自動切換至計費代理 API。
- **[優化]** 紀錄顯示來源標籤，讓您清楚知道每筆資料是「免費抓取」還是「API 計費」。

### [2026-04-05] - 三核心 API 究極版 V20
- **[重大更新]** **總覽頁自動批次抓取 (Batch Mode)**：支援直接貼入 PCC 搜尋結果網址，自動抓取前 5 筆非重複標案。
- **[智慧排查]** 系統自動預先對比總覽頁名稱與試算表資料，避免重複抓取以節省三家 API 的額度。
- **[安全機制]** 內建 **10 秒冷卻間隔** 與進度條顯示，完全防範被目標網站偵測異常流量。

### [2026-04-05] - 三核心 API 究極版 V19
- **[重大更新]** **標案名稱自動超連結**：Google Sheets 中的 B 欄位 (Name) 改為 `HYPERLINK` 公式，點擊標案名稱即可直接開啟原始網址。
- **[優化]** GAS 後端邏輯調整，確保網址正確傳遞並寫入試算表。

### [2026-04-05] - 三核心 API 究極版 V18
- **[重大更新]** **響應式佈局 (Responsive Design)**：全介面優化，支援手機、平板與電腦不同螢幕尺寸。
- **[優化]** 調整行動版網頁字級與欄位排版，確保在小螢幕上也能清晰閱讀抓取與額度資訊。
- **[優化]** 強化背景裝飾與 UI 組件的流動性，提升不同解析度下的視覺體驗。

### [2026-04-05] - 三核心 API 究極版 V17
- **[重要]** 修復三家 API (WebScraping.AI, ScrapingAnt, scrape.do) 額度查詢欄位不對齊問題。
- **[新增]** 即時顯示三大 API 的 **「總額度」** 與 **「重置日期時間」** (儀表板全功能)。
- **[優化]** UI 卡片排版，自動隱藏/縮合長標題，確保在行動端也能完美顯示細節資訊。
- **[穩定]** 強化 GAS 後端錯誤處理機制，防止 API 呼叫失敗時中斷正常流程。

---
*Developed with ❤️ by Antigravity AI for HILTI.*
